using System;
using System.Collections.Generic;
using System.Text;

namespace Geometry
{
    class Parralepiped
    {
        // volume of slanted parallepiped (parralel-box) for given edge lengths and angle from vertical = abs(dot(edgeUp, cross(edgeForward, edgeRight))

        // area of a 2D parralellogram in 3D space (and square/rectangle) can be calculated with area = cross(edge1, edge2)
    }
}
