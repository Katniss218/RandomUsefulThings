using System;
using System.Collections.Generic;
using System.Text;

namespace RandomUsefulThings.Physics
{
    class Moment
    {
        // Moment describes how much a given force causes the object to turn.

        // Unit: [N*m] or [ft*lb]     ie newton-meter or foot-pound

        // Moment = PerpendicularForce * distanceFromPivot

        // if the force is not perpendicular, we can split it into perpendicular and parallel components along the vector from point where the force is applied to the pivot.
    }
}
